{% extends 'admin/indexAdmin.html.twig' %}

{% block title %}Crear cuenta
{% endblock %}

{% block main %}
	<h3>Crear Cuenta de Administrador/a</h1>

	{{ form_errors(registroForm) }}

	{{ form_start(registroForm) }}
	{{ form_row(registroForm.email) }}
	{{ form_row(registroForm.confirmacion) }}
	{{ form_end(registroForm) }}
{% endblock %}
